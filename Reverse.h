#ifndef REVERSE_H
#define REVERSE_H
#include <iostream>
#include <string>
#include <math.h>
using namespace std;

class Reverse {
public:
    int reverseDigit(int value);
    string reverseString(string letters);
};
#endif //REVERSE_H
